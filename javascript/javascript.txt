Javascript

👉 Programming Language
👉 Makes interactivity in web page

 3 ways to connect with HTML 
 1. inline
 2. internal 
 3. External 

 var , let , const are keywords to declare variables


 Scope
 ------
 var 
---
 1. redeclare
 2. reassign
 3. Scope : Function/Global  Scope
 4. hoisting - initialized as Undefined
 5. initial value not required

 let 
----
 1. no - redeclare
 2. re -assign
 3. Scope : Block Scope
 4. hoisting -Uninitialized (TDZ)
 5. initial value not required

 const 
 -----
 1. no - redeclare
 2. no - reassign 
 3. Scope : Block Scope
 4. hoisting - Uninitialized (TDZ)
 5. initial value  required


 Variable Scope
 1. global 
 2. functional 
 3. block 


Data Types

👉 Primitive (basic types):

    Number → 42, 3.14

    String → "Hello"

    Boolean → true, false

    Undefined → variable declared but no value yet

    Null → intentional "nothing"

    NaN  → Not a Number

    Symbol → unique value (used rarely)

    BigInt → very large numbers

👉 Non-primitive (objects):

	Array → [1, 2, 3]
    Object → {name: "Denny", age: 25}
    Function → function greet() { ... }

to find the data type -> typeof

Type coercion
1. implicit
2. explicit

    Three Types of Coercion
        Strings, Numbers, and Booleans.
    1. String:
        (JavaScript converts the non-string operand into a string and concatenates them)
    "5"+2 =52
    console.log("false"+true) falseTrue
    10 + 20 + "30"

    2 Number:
        => ( -, *, /, % or comparison operators )
        "5" - 2 results in 3 (the string is converted to a number)
        "5" * "2" results in 10
        true + 1 results in 2 (booleans convert to numbers: true -> 1, false -> 0)
        "hello" * 2 results in NaN 

    3. Boolean Coercion
    Happens in logical contexts like if statements, ternary operators, 
    or when using logical operators (&&, ||, !). 
    JavaScript classifies all values as either Truthy or Falsy
    falsy:
    0
    ""
    Null
    Undefined
    NaN


    a="madam";
rev='';
for(i of a){
    console.log(i)
}

simple function
syntax:

function function_name(){ // parameter
    // stattement
}

function_name() // argument

function add(a,b=5){
    return a+b
}

console.log(add(5,10))


2. arrow function
const add=()=>{}

const add=(a,b)=>{console.log(a+b)}
add(5,10)

3. Anonymous function
const add = function(){

}
add()

const add=function(a,b){
    console.log(a+b)
}
add(5,10)

4. callback function
A function that is passed as an argument to another function to run later
function greet(bhuvana){
    console.log("welcome")
    bhuvana()
    console.log("End")

}

function add(){
    console.log(5+10)
}

greet(add);

5. higerorder function
    receive / return a function 

function outer(){
    console.log("outer function")
    function inner(){
        console.log("inner function")
    }
    return inner;
}

let outr=outer()
outr()

Closure
    remember/retain  the outer function variable
    function outer(){
    let a=0
    function inner(){
        console.log(a++)
    }
    return inner;
}

let outr=outer()
outr()
outr()

a="name"
console.log(a.length)
console.log(a[0])

for(let i=0;i<=a.length;i++){
    console.log(a[i])
}

a="apple"
b=" Orange "
console.log(a.toUpperCase())
console.log(a.toLowerCase())
console.log(a.slice(1,4))
console.log(b)
console.log(b.trim())
c="I like javascript"
console.log(c.replace("like","love"))
console.log(c.split(' '))
console.log(c.includes("love"))
console.log(c.indexOf('a'))
console.log(c.lastIndexOf('a'))
console.log(c.charAt(4))
d="and python"
console.log(c.concat(d))
console.log(b)
console.log(b.trim())
c="I like javascript"
console.log(c.replace("like","love"))
console.log(c.split(' '))
console.log(c.includes("love"))
console.log(c.indexOf('a'))
console.log(c.lastIndexOf('a'))
console.log(c.charAt(4))
d="and python"
console.log(c.concat(' ',d))


Array:
Mixed,number,string,object in array,2d
console.log(nums.map((num)=>{return num*100}));
console.log(nums.filter((num)=>num%2==0));
console.log(nums.reduce((accumulator,currentelement)=>{return acc+currentelement},0))


const person = {
  name: "apple",   
  age: 25,
  isDeveloper: true,
  skills: ["JS", "Python"] 
};


Spreed
--------
const fruits = ["apple", "orange"];
const newFruits = [...fruits, "mango", "banana"];

console.log(newFruits);

const user = { name: "Ram", age: 25 };

const updatedUser = { ...user, city: "Chennai" };

console.log(updatedUser); 


rest
-------
function sum(...numbers) {
  console.log(numbers); 
  
  return numbers.reduce((total, num) => total + num, 0);
}

console.log(sum(10, 20, 30, 40)); 

destructuring:
array:
const fruits = ["apple", "orange", "banana"];
const [first, second, third] = fruits;

console.log(first);  
console.log(second); 
console.log(third);  


object:

const person = { name: "Karthik", age: 26, city: "Madurai", role: "Developer" };

const { name, age, ...restOfData } = person;

console.log(name); 
console.log(restOfData);